import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:appetit/app/modules/novo_pedido/selecionar_data/selecionar_data_page.dart';

void main() {
  testWidgets('SelecionarDataPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SelecionarDataPage(title: 'SelecionarData')));
    //  final titleFinder = find.text('SelecionarData');
    //  expect(titleFinder, findsOneWidget);
  });
}
