import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:appetit/app/modules/listagem_pedidos/listagem_pedidos_page.dart';

void main() {
  testWidgets('ListagemPedidosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ListagemPedidosPage(title: 'ListagemPedidos')));
    //  final titleFinder = find.text('ListagemPedidos');
    //  expect(titleFinder, findsOneWidget);
  });
}
